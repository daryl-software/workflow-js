{
  "name": "workflow-js",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node lib/index.js",
    "build": "tsc",
    "start-debug": "tsc && node --trace-uncaught lib/index.js"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.8.3",
    "@types/node": "^13.11.0"
  }
}
